# Integrations

External clients that connect massCode to other tools live here.

Examples:

- `clipper/`
- `raycast/`
- `cli/`
- `vscode/`

Integration clients should talk to massCode through the local integration API.
They must not read or write the vault directly.
